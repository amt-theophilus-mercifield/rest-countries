import styled from "styled-components"

interface Props {
    
}

export const Countries = (props: Props) => {
    return (
        <Main>
            
        </Main>
    )
}

const Main = styled.main``
